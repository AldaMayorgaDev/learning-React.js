import { createTheme } from "@mui/material";

export const purpleTheme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#5c28b9',
        },
        secondary: {
            main: '#f50057',
        },
        error: {
            main: '#e91e63',
        },
    },
})